# ── Local docs / planning notes — NEVER commit (code only) ──
# All planning docs, design notes, and generated markdown stay local-only.
# The repository holds source code only; no docs, plans, or process notes.
/docs/
*.plan.md
PLAN*.md
IMPLEMENTATION_PLAN.md
BACKEND_ANALYSIS.md

# ── Secrets (NEVER commit) ─────────────────────────────
.env
*.env
!*.env.example
backend/**/.env

# ── Python / Django backend ────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
*.sqlite3
db.sqlite3
*.log
staticfiles/

# ── Android / Gradle ───────────────────────────────────
*.iml
.gradle/
local.properties
/build/
**/build/
.cxx/
captures/
*.apk
*.aab
*.keystore
!debug.keystore

# ── IDE / OS ───────────────────────────────────────────
.idea/
.DS_Store
Thumbs.db
